import 'package:flutter/material.dart';

class UserChatQuestionWidget extends StatelessWidget {
  const UserChatQuestionWidget({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: ListTile(),
    );
  }
}
